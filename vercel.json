{
  "version": 2,
  "installCommand": "npm install -g pnpm && pnpm install --no-frozen-lockfile",
  "buildCommand": "cd apps/web && npx prisma generate && pnpm build",
  "outputDirectory": "apps/web/.next",
  "framework": null,
  "env": {
    "NODE_ENV": "production"
  }
} 